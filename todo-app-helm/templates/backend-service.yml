apiVersion: v1
kind: Service

metadata:
  name: todo-backend
  namespace: {{ .Values.namespace }}
  labels:
    app: multi-services

spec:
  selector:
      app: multi-services
      type: backend
  ports:
    - port: 3000
      targetPort: 3000
