apiVersion: v1
kind: Secret

metadata:
  name: {{ .Release.Name }}-db-secret
  namespace: {{ .Values.namespace }}
  labels:
    app: multi-services

type: Opaque
data:
  mongo-url: {{ .Values.mongodb.url | b64enc | quote  }}
