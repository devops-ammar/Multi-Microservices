apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-ingress-frontend
  namespace: {{ .Values.namespace }}
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: todo-frontend
                port:
                  number: 80
